<Window x:Class="WpfApp4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp4"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel>

            <Border Padding="10" Margin="4" BorderThickness="1" BorderBrush="Gray"
            Height="210" Width="210">
                <Image Source="{Binding SelectedItem.Source, ElementName=ImageChooser}"/>
            </Border>
            <ListBox HorizontalAlignment="Center" x:Name="ImageChooser" BorderThickness="0">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <Image Source="Resources/male-icon.png" Width="64" Margin="4"/>
                <Image Source="Resources/female-icon.png" Width="64" Margin="4"/>
            </ListBox>
        </StackPanel>
    </Grid>
</Window>
